<!--component/tabs/tab.wxml-->
<view class='period'>
  
    <view 
    wx:for="{{tabList}}" 
    class="item {{ activeTab == item.id ? 'bluecss':''}}" 
    wx:key="id" 
    bindtap="changeType" 
    data-type="{{item.id}}">  {{item.text}} </view>
</view>